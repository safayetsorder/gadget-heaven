import React from 'react';

const Wishlist = () => {
    return (
        <div>
            <h1 className='font-bold text-3xl mt-12'>
                Wishlist
            </h1>
            {/* { Wishlist }
            {Wishlist.map(p => <p key={p.product_id}>{p.product_title}</p>)} */}
        </div>
    );
};

export default Wishlist;